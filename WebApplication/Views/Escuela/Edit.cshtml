@model Application.Dtos.EscuelaDtoForUpdate


    <form action="@Url.Action("Edit","Escuela")" method="post">
        <input asp-for="Nombre"/>
        <button class="btn btn-success" type="submit">Editar</button>
        <a class="btn btn-secondary" href="@Url.Action("Index","Escuela")">Cancelar</a>
    </form>
